apiVersion: apps/v1
kind: Deployment
metadata:
  name: kitchen-service
  namespace: foodflux
spec:
  replicas: 2
  selector:
    matchLabels:
      app: kitchen-service
  template:
    metadata:
      labels:
        app: kitchen-service
    spec:
      containers:
        - name: kitchen-service
          image: foodflux/kitchen-service:latest
          imagePullPolicy: Never
          env:
            - name: DB_HOST
              value: postgres
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: orders_db
            - name: DB_USER
              value: admin
            - name: DB_PASSWORD
              value: password
            - name: KAFKA_BROKERS
              value: kafka:29092
            - name: PORT
              value: "3004"
          ports:
            - containerPort: 3004
          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: kitchen-service
  namespace: foodflux
spec:
  selector:
    app: kitchen-service
  ports:
    - port: 3004
      targetPort: 3004
  type: ClusterIP
